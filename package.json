{
  "name": "self-service-ordering",
  "version": "0.1.0",
  "bin": {
    "self-service-ordering": "bin/self-service-ordering.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.1023.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.859.0",
    "@aws-sdk/lib-dynamodb": "^3.859.0",
    "aws-cdk-lib": "2.206.0",
    "constructs": "^10.0.0"
  }
}
