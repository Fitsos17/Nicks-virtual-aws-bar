Customer Routes:
  / (redirect every incorrect route to welcome page "/")
  catalog (they can query each item with id. If they get the whole calatlog they can get just
  the ingrediants of the drink. If they query the drink they can get additional info)
  tables
  order
  -> [{
        "route": "catalog",
        "methods": ["GET"]
      },
      {
        "route": "tables",
        "methods": ["GET", "POST"]
      },
      {
        "route": "order",
        "methods": ["GET", "POST"]
      }]

Waiter, Barman, Admin routes:
  to be developed